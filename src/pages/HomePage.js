import React from 'react';
import Hero from '../elements/Hero';

const HomePage = () => {
    return (
        <div className="homepage">
            <Hero />
        </div>
    );
};

export default HomePage;